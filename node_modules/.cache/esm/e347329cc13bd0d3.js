let start,dbConnection;_c13‍.w("./src/core/app",[["start",["start"],function(v){start=v}]]);_c13‍.w("./src/core",[["dbConnection",["dbConnection"],function(v){dbConnection=v}]]);

const dotenv = require('dotenv');
if (process.env.NODE_ENV !== 'production') dotenv.config();


dbConnection().then(res=>{
    start();
})
