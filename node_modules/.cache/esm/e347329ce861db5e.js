let start,app,dbConnection;_e86‍.w("./src/core",[["start",["start"],function(v){start=v}],["app",["app"],function(v){app=v}],["dbConnection",["dbConnection"],function(v){dbConnection=v}]]);const dotenv = require('dotenv');
if (process.env.NODE_ENV !== 'production') dotenv.config();


dbConnection().then(res=>{
    start();
})
